# SMTP config (use provider SMTP — SendGrid, Mailgun, SES, or your SMTP server)
SMTP_HOST=smtp.sendgrid.net
SMTP_PORT=587
SMTP_USER=apikey            # for SendGrid use "apikey"
SMTP_PASS=YOUR_SENDGRID_KEY
SMTP_SECURE=false           # true if using port 465
FROM_EMAIL=rohit@example.com
FROM_NAME=Rohit Bhatu

# Message / recipient
TO_EMAIL=hr@company.com
HIRING_MANAGER_NAME=Hiring Manager

# Optional runtime values
SENDER_PHONE=+91-XXXXXXXXXX
RESUME_PATH=./src/assets/rohit-resume.pdf
